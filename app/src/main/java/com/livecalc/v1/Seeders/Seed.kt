package com.livecalc.v1.Seeders

class Seed {
    /*fun v1(): InputData {
        val products: HashMap<Int, Product> = hashMapOf()

        for (i in 1..3) {
            products[i] = Product(
                i,
                false,
                10,
                10,
                2.00F,
                2.00F,
                0.0F,
                0.0F
            )
        }

        val discounts: HashMap<Int, DiscountInput> = hashMapOf(
            10 to DiscountInput(
                10,
                false,
                DiscountApplyConditionType.SAME_AMOUNT,
                1.0F,
                DiscountApplyToType.EACH_PRODUCT,
                DiscountFormula.VALUE,
                0.09F,
                DiscountResultType.PRODUCTS_SAME,
                listOf(),
                listOf(1, 2, 3),
                hashMapOf(5 to ProductSelected(1, 0.0F, 3)),
                DiscountPriceToApplyType.DISCOUNTED_PRICE,
                false
            )
        )

        val orderSettings = OrderSettings(
            splitRatioJuridical = 20.0F
        )

        return InputData(
            products,
            discounts,
            orderSettings
        )
    }

    fun v2(): InputData {
        val products: HashMap<Int, Product> = hashMapOf()

        for (i in 1..5) {
            products[i] = Product(
                i,
                false,
                10,
                1,
                2.0F,
                2.00F,
                0.0F,
                2.00F
            )
        }

        val discounts: HashMap<Int, DiscountInput> = hashMapOf(
            10 to DiscountInput(
                10,
                false,
                DiscountApplyConditionType.SAME_AMOUNT,
                1.0F,
                DiscountApplyToType.EACH_PRODUCT,
                DiscountFormula.VALUE,
                1F,
                DiscountResultType.PRODUCTS_SAME,
                listOf(20),
                listOf(1, 2, 3),
                hashMapOf(4 to ProductSelected(1, 0.0F, 3)),
                DiscountPriceToApplyType.BASIC_PRICE,
                false
            )
        )

        val orderSettings = OrderSettings(
            splitRatioJuridical = 0.0F
        )
        return InputData(
            products,
            discounts,
            orderSettings
        )
    }*/
}